apiVersion: v1
kind: Service
metadata:
  name: {{ include "example-rpc.fullname" . }}
  labels:
    {{- include "example-rpc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "example-rpc.selectorLabels" . | nindent 4 }}
