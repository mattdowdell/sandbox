// Code generated by protoc-gen-connect-go-v1.18.1. DO NOT EDIT.
//
// Source: authn/v1/service.proto

package authnv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/mattdowdell/sandbox/gen/authn/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// AuthnServiceName is the fully-qualified name of the AuthnService service.
	AuthnServiceName = "authn.v1.AuthnService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// AuthnServiceLoginProcedure is the fully-qualified name of the AuthnService's Login RPC.
	AuthnServiceLoginProcedure = "/authn.v1.AuthnService/Login"
	// AuthnServiceAuthenticateProcedure is the fully-qualified name of the AuthnService's Authenticate
	// RPC.
	AuthnServiceAuthenticateProcedure = "/authn.v1.AuthnService/Authenticate"
)

// AuthnServiceClient is a client for the authn.v1.AuthnService service.
type AuthnServiceClient interface {
	// Login is used to generate a token to use as authentication in subsequent requests. This method
	// should be used by external API clients.
	Login(context.Context, *connect.Request[v1.LoginRequest]) (*connect.Response[v1.LoginResponse], error)
	// Authenticate is used to validate tokens generated by Login. This method should be used
	// internally for all authenticated endpoints.
	Authenticate(context.Context, *connect.Request[v1.AuthenticateRequest]) (*connect.Response[v1.AuthenticateResponse], error)
}

// NewAuthnServiceClient constructs a client for the authn.v1.AuthnService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAuthnServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AuthnServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	authnServiceMethods := v1.File_authn_v1_service_proto.Services().ByName("AuthnService").Methods()
	return &authnServiceClient{
		login: connect.NewClient[v1.LoginRequest, v1.LoginResponse](
			httpClient,
			baseURL+AuthnServiceLoginProcedure,
			connect.WithSchema(authnServiceMethods.ByName("Login")),
			connect.WithClientOptions(opts...),
		),
		authenticate: connect.NewClient[v1.AuthenticateRequest, v1.AuthenticateResponse](
			httpClient,
			baseURL+AuthnServiceAuthenticateProcedure,
			connect.WithSchema(authnServiceMethods.ByName("Authenticate")),
			connect.WithClientOptions(opts...),
		),
	}
}

// authnServiceClient implements AuthnServiceClient.
type authnServiceClient struct {
	login        *connect.Client[v1.LoginRequest, v1.LoginResponse]
	authenticate *connect.Client[v1.AuthenticateRequest, v1.AuthenticateResponse]
}

// Login calls authn.v1.AuthnService.Login.
func (c *authnServiceClient) Login(ctx context.Context, req *connect.Request[v1.LoginRequest]) (*connect.Response[v1.LoginResponse], error) {
	return c.login.CallUnary(ctx, req)
}

// Authenticate calls authn.v1.AuthnService.Authenticate.
func (c *authnServiceClient) Authenticate(ctx context.Context, req *connect.Request[v1.AuthenticateRequest]) (*connect.Response[v1.AuthenticateResponse], error) {
	return c.authenticate.CallUnary(ctx, req)
}

// AuthnServiceHandler is an implementation of the authn.v1.AuthnService service.
type AuthnServiceHandler interface {
	// Login is used to generate a token to use as authentication in subsequent requests. This method
	// should be used by external API clients.
	Login(context.Context, *connect.Request[v1.LoginRequest]) (*connect.Response[v1.LoginResponse], error)
	// Authenticate is used to validate tokens generated by Login. This method should be used
	// internally for all authenticated endpoints.
	Authenticate(context.Context, *connect.Request[v1.AuthenticateRequest]) (*connect.Response[v1.AuthenticateResponse], error)
}

// NewAuthnServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAuthnServiceHandler(svc AuthnServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	authnServiceMethods := v1.File_authn_v1_service_proto.Services().ByName("AuthnService").Methods()
	authnServiceLoginHandler := connect.NewUnaryHandler(
		AuthnServiceLoginProcedure,
		svc.Login,
		connect.WithSchema(authnServiceMethods.ByName("Login")),
		connect.WithHandlerOptions(opts...),
	)
	authnServiceAuthenticateHandler := connect.NewUnaryHandler(
		AuthnServiceAuthenticateProcedure,
		svc.Authenticate,
		connect.WithSchema(authnServiceMethods.ByName("Authenticate")),
		connect.WithHandlerOptions(opts...),
	)
	return "/authn.v1.AuthnService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AuthnServiceLoginProcedure:
			authnServiceLoginHandler.ServeHTTP(w, r)
		case AuthnServiceAuthenticateProcedure:
			authnServiceAuthenticateHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAuthnServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedAuthnServiceHandler struct{}

func (UnimplementedAuthnServiceHandler) Login(context.Context, *connect.Request[v1.LoginRequest]) (*connect.Response[v1.LoginResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("authn.v1.AuthnService.Login is not implemented"))
}

func (UnimplementedAuthnServiceHandler) Authenticate(context.Context, *connect.Request[v1.AuthenticateRequest]) (*connect.Response[v1.AuthenticateResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("authn.v1.AuthnService.Authenticate is not implemented"))
}
