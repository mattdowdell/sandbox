---
# docker-compose variant of ../k8s/helm/grafana/values.yaml (dashboardProviders section).

apiVersion: 1
providers:
  - name: dashboards
    type: file
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: true
